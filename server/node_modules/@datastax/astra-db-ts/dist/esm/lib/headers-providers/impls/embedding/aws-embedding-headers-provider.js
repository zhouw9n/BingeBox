// Copyright Datastax, Inc
// SPDX-License-Identifier: Apache-2.0
import { StaticHeadersProvider } from '../../../../lib/headers-providers/index.js';
export class AWSEmbeddingHeadersProvider extends StaticHeadersProvider {
        constructor(accessKeyId, secretAccessKey) {
        const headers = {
            'x-embedding-access-id': accessKeyId,
            'x-embedding-secret-id': secretAccessKey,
        };
        super(headers);
    }
}
